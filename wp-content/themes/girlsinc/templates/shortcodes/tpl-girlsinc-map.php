<?php
/**
 * This has a bunch of different moving parts, so it has its own directory
 * We'll load the map entry point from there instead to prevent exposing the rest through shortcode calls
 */

get_template_part('templates/map/tpl-girlsinc-map');